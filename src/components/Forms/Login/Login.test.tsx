import { describe, it } from 'vitest';

describe('Login', () => {
  it('renders the component', () => {
    // render(<Login onSubmit={({ email, password }) => {}} />);
    // Add your tests here
  });
});
