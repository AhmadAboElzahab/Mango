import { render, screen } from '@testing-library/react';
import Toolbar from './Toolbar';

describe('Toolbar', () => {
  it('renders the component', () => {
    render(<Toolbar />);
    // Add your tests here
  });
});
